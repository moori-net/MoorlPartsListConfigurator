name: Update docs
on:
    workflow_dispatch:
    push:
        branches:
            - 6.7
        paths:
            - docs/**

jobs:
    build:
        uses: moorl/github-actions/.github/workflows/docs.yml@main
        with:
            extensionName: ${{ github.event.repository.name }}
        secrets:
            docsToken: ${{ secrets.DOCS_TOKEN }}
